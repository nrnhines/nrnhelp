?p neuron.exe general classes
?1 ValueFieldEditor
@hsyn
vfe = new ValueFieldEditor(...)
@hdesc
Takes exactly the same args as @axvalue or @axpvalue but is an object
which can change some features (default) under program control.
Note that when this object is created, there must be an open @axpanel .
@h
? default
@hsyn
vfe.default
@hdesc
If the field editor is a default field editor then the current value
becomes the default value. ie. the red checkmark is turned off.
@h

